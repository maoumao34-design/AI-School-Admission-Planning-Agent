# AI 升学规划 Agent · 高考志愿 — 个人 / 小组报告（交付物 3）

> **方向 05 · 决策型 Agent**。本报告对应 [DELIVERY-STANDARD.md](../DELIVERY-STANDARD.md)「个人/小组报告」七项 + [TASK-SPEC.md](../TASK-SPEC.md) §7 要求。
>
> - 🌐 **Live（交付物 1）**：<https://ai-school-admission-planning-agent.vercel.app/>
> - 📦 **源码（交付物 2）**：[GitHub 仓库](https://github.com/maoumao34-design/AI-School-Admission-Planning-Agent)，`main` = `57a7d01`（验收前可能继续推进）
> - 👤 **完成方式**：本项目为**单人（maozh2）**完成，"成员分工"由人 + 6 个智能体（升学规划PM / AI工程师 / 全栈 / 数据 / QA / 群管理）协作分工共同填写（见 §10）。

---

## 0. 摘要（一页结论）

| 项 | 结论 |
|---|---|
| **做了什么** | 一个面向高考志愿的**决策型 Agent**：输入考生条件 → 资格校验 → 候选/方案比较 → 改条件重算 → 导出带官方来源的行动方案。范围 = 江苏 2026 **物理类 + 历史类**两类。 |
| **三交付物状态** | ① 可访问网页 = vercel.app（已部署、全球可访问）✅ ② 完整源码 + README = `main`（`57a7d01`）✅ ③ 本报告 ✅（验收后定稿） |
| **完成程度** | **决策核心（资格/比较/重算/概率/导出/官方来源）已真实实现并运行**；TASK-SPEC §三 4 核心功能：①**对话建条件**（确定性层、不用 LLM key，`/api/condition-building`，MAO-23）+ **对话每轮更新考生档案**（MAO-25 返工）已上；②资格校验 ✅；③两套方案并排 ✅（MAO-24）；④计划版本部分（关键日期/风险提醒补强中）。**多因素推荐理由**（位次+选科+预算+年份 trace，`17fe3e9`）已上。**伪登录 + 账户档案分桶隔离**（MAO-21，`c8e557f`）已上。**数据双类 3666 卡**（物理 996 校 / 历史 803 校，jseea 官方 PDF 批量解析）。**MAO-26 投档分差兜底分档已推待合 main**（`bd384a1`，解锁 3666 卡冲稳保，npm test 78/78）。**仍 pending**：全栈③计划版本细节、一分一段表→位次回填（OCR 墙）。 |
| **灵魂** | 可解释、可调整、**不承诺录取**（概率只标注方法 + 数据年份，非预测）。 |

---

## 1. 问题与用户

> 对应 DELIVERY §一-1「问题与用户」/ TASK-SPEC §2、§7「场景与范围」。

- **目标用户**：参加高考、需要填报志愿的**考生及家长**（核心）；延伸到**升学规划人员**（一个账号管理多个考生档案，**已纳入并实现伪登录 + 账户档案分桶隔离**，MAO-21）。
- **真实场景**：江苏 2026 物理类考生，分数 / 位次 / 选科 / 预算已知，需要在众多院校专业组中筛出**符合硬性资格**、**概率档合理**、**可并排比较**的候选，并在分数/预算/目标变化时快速看到方案变化。
- **三个核心痛点**（TASK-SPEC §2）：
  1. **信息分散且会变化**——院校/专业/计划数/费用/截止时间散落不同官方页面，往年信息不能直接替代本周期。
  2. **资格条件容易遗漏**——选科/学历/先修课/材料/费用等硬条件必须先判，不能凭感觉推荐。
  3. **方案需要持续调整**——分数/预算/地区/目标变化时，候选/差距/风险/时间线要一起变。
- **为何聚焦此范围**：高考志愿是信息高度分散、资格判定强、容错低的典型决策场景；限定**一个省（江苏）+ 一个年度（2026）+ 物理类/历史类两类**（maozh2 要求覆盖大部分考生）可在数据可得性内把"真实来源 + 真规则 + 真重算"做透，而非摊大饼。**不做博士/留学**（范围红线）。
- **成功标准**：代表性案例（江苏 2026 物化 637/5,200）能完整跑通 6 步；改关键条件后候选/方案/依据随之更新；每个结论可展开看依据、规则、官方来源。

---

## 2. 方案与交互

> 对应 DELIVERY §一-2「方案与交互」/ TASK-SPEC §3、§4、§5。

### 2.1 核心任务流程（端到端 6 步）

| 步骤 | 内容 | 页面 / API |
|---|---|---|
| **01 建立条件** | 填写/追问影响资格的缺失条件与冲突 | **两种模式**：`ConditionForm`（结构化表单）或 `ChatPanel`（自然语言对话 → `/api/condition-building` 确定性层：抽条件 / `findMissingConditions` / 冲突检测 / 追问，不用 LLM key；ready 后建立/更新考生档案并驱动下游） |
| **02 获取官方信息** | 展示本省本年度真实数据 + 原始来源 | 候选卡 `source.url` + `updated` + `status` |
| **03 资格过滤** | 逐条规则判定，排除不满足硬条件者 | `POST /api/eligibility`（阻断/advisory/needs_review 可展开） |
| **04 方案比较** | 候选/差距/成本/风险 + 多策略并排 | `POST /api/compare`（院校优先/专业优先/均衡排序） |
| **05 改条件重算** | 改一项分数/预算/地区/目标，重跑过滤+规划，对比前后版本 | `POST /api/recompute`（`diff.added/removed/changed`）；前端 debounce 300ms 并行重调 |
| **06 确认导出** | 确认最终方案，导出带来源的行动计划 | `exportReport()` → Markdown（条件/资格/方案/每条来源+时间/三红线声明）→ `.md` 下载 |

### 2.2 对话区 / 工作区配合

- **左侧建条件区**（`ConditionForm`）：结构化收集影响资格的条件；任一字段变化触发随动重算。
- **右侧工作区**（`Workspace` + `CandidateCard`）：结构化候选卡（院校/专业组、批次/层次标签、参考概率档、近 3 年计划数/最低分/位次差、选科要求、学费、推荐理由可展开、官方来源入口、异常提示）。
- **每个响应都带 `trace`**：`conditions_used` / `rules_applied` / `dataset_year` / `generated_at`（决策透明化）。

### 2.3 关键状态 / 异常状态

- `info_insufficient`：缺关键条件 → 给原因 + 下一步。
- `no_result`：无候选通过硬条件 → 给原因 + 下一步。
- `needs_review`：数据待抽取（`tuition`/`plan=null`）或未知规则类型 → **不阻断**，附 `caveat` 标记（避免样本缺值误杀）。

### 2.4 高价值交互设计

**「改条件 → 候选/档位随动 + 前后版本差异」**：改分数/位次/预算后，并行重调 `/api/compare` + `/api/eligibility`，候选、概率档、位次差、资格全部随动；`/api/recompute` 产出 `added/removed/changed` 版本差异。让"方案需要持续调整"这一痛点在页面实时可见。

---

## 3. 实现与技术

> 对应 DELIVERY §一-3「实现与技术」。只写真正用到的。

| 层 | 技术 | 用途 |
|---|---|---|
| **框架** | Next.js（App Router）一体全栈 | 前端 + API Routes 同仓；Vercel 部署 |
| **语言** | TypeScript | 端到端类型安全 |
| **样式** | Tailwind CSS | 响应式布局（含移动端） |
| **决策引擎** | TS **纯函数**（`src/decision/*`） | 资格校验 / 概率档 / 方案排序（**先过资格再排序**，per-card 选科）/ 改条件重算 / 版本差异 / 对话建条件（`/api/condition-building` 确定性层）/ 多因素推荐理由（位次+选科+预算+年份 trace）；输入相同输出相同，`vitest` 73 用例全绿 |
| **数据** | JSON（`data/*.json`） | 真实官方来源结构化；字段契约与 `src/decision/types.ts` 一致，引擎直接消费；**双类 3666 卡**（物理 2511 / 996 校 + 历史 1155 / 803 校），由 jseea 2025 投档线 PDF 经 pymupdf 批量解析 + converter 脚本转卡 |
| **概率方法** | 位次差法（确定性算术）+ 投档分差兜底 | **非 LLM、非预测**，只标 `method` + `data_years`。`min_rank` 有值→位次差法（2024/2025 主依据）；`min_rank` 空→投档分差兜底分档（MAO-26 进行中），reason/概率标注当前用哪种方法、不混淆 |
| **持久化/鉴权（增强项）** | Supabase（Postgres + Auth + RLS）+ Prisma | 登录/多账户数据隔离；**核心流程零环境变量、不依赖它即可部署运行** |
| **部署** | Vercel（连 GitHub 自动部署） | 合并 `main` 自动重部署；`vercel.json` 固定 `prisma generate && next build` |

**关键代码位置**：引擎 `src/decision/{engine,types,adapter,handlers}.ts`；API `app/api/{compare,eligibility,recompute}/route.ts`；前端 `app/page.tsx` + `src/components/*`；数据 `data/sample-jiangsu-2026-phys.json` + `data/rules.example.json`；数据模型 `prisma/schema.prisma`（Account→Profile→Plan→Version）。

---

## 4. 数据来源与规则

> 对应 TASK-SPEC §7「数据年份和来源 / 资格与比较规则」。完整明细见 [`data/DATA-PACKAGE.md`](../data/DATA-PACKAGE.md) 与 [README §数据来源/资格规则](../README.md)。

### 4.1 数据（江苏 2026 物理类 + 历史类，状态：投档分权威 / 位次待补）

- **范围**：江苏 · 2026 · **物理类 + 历史类两类**（maozh2 要求覆盖大部分考生；示例考生 637 / 5,200 / 物+化）。
- **数据集**：`data/sample-jiangsu-2026-phys.json`（物理类）+ 历史类卡，共 **3666 张专业组卡**（物理 2511 / 996 校，历史 1155 / 803 校），投档分全段覆盖（物理 463–686 / 历史 482–679），`subject_requirement` 用「物理+化学 / 历史+思想政治 / 不限」格式引擎 per-card 直消费。由全栈 pymupdf 解析 jseea 2025 投档线 PDF（物理册 2522 条 / 历史册 1155 条）→ converter 脚本批量转卡，来源全标 jseea 官方 URL。
- **年份与来源**：投档最低分（江苏省教育考试院 jseea 2025 投档线 PDF，**权威**，2024 回填中）；**最低位次 `min_rank`**：仅 61 条手填基线卡有值，3621 条 null 标「位次待一分一段表派生」（一分一段表为图片、工具链暂无 OCR，引擎走投档分差兜底，位次后续补精）；招生计划/学费/majors/城市 待《招生计划》回填。

### 4.2 资格规则（确定性纯函数，不交给 LLM）

每条规则 = 文本规则 → 可机读判据 `machine{type, params}`，引擎 canonical 4 类（见 README 资格规则表）：

| rule_id | 类别 | machine.type | 判据 |
|---|---|---|---|
| `SUBJ-REQ-2024-001` | 选科 | `subject_match` | required:[物理,化学] |
| `BATCH-QUAL-2026-001` | 批次 | `batch` | allowed:[普通类本科批] |
| `FEE-CAP-2026-001` | 费用 | `tuition_le` | max:60000 |
| `PLAN-AVAIL-2026-001` | 计划 | `plan_gt` | min:0 |

**判定原则**：只有「能确定不满足硬条件」才阻断；待抽取/未知类型一律不阻断、改附 `caveat` + `needs_review`。

---

## 5. 完成范围与限制（真实 / 样本 / 模拟 显式标注）

> 对应 DELIVERY §一-4 + 红线「模拟内容明确标注，不作为已实现功能」。

### ✅ 已真实实现并运行

- **决策核心引擎**：资格校验 / 概率档 / 方案排序 / 改条件重算 / 版本差异 / 对话建条件 / 多因素推荐理由 = TS 纯函数（`vitest` 73/73）。
- **§三 ① 对话建条件（确定性层，不用 LLM key）**：`/api/condition-building` 抽条件 + `findMissingConditions` + 冲突检测 + 追问；**对话每轮把条件实时合并进当前档案、右侧随重算**，ready 后新建完整档案（MAO-23 引擎 + MAO-25 建档返工，`3b4584c`）。
- **§三 ② 资格校验**：`POST /api/eligibility`，per-card `subject_requirement` 硬过滤（首选+再选均核）。
- **§三 ③ 两套方案并排**：compare API 传全部策略 → 「院校优先 / 专业优先」双列并排、选中高亮（MAO-24，`aa090af`）。
- **多因素推荐理由**：每张卡 reason 同时含 位次差+冲稳保档 / 选科（要求 vs 考生→符合/不符/待核）/ 预算（学费 vs 上限）/ 参考年份+非预测声明（`17fe3e9`，73/73）。
- **数据（双类 3666 卡）**：物理 2511 / 996 校 + 历史 1155 / 803 校，jseea 2025 投档线 PDF 经 pymupdf 批量解析转卡，来源全标官方。
- **概率 + 候选随动**：位次差法/投档分差确定性算术——**换分数/位次/选科，候选集合随之变化**（QA 实测：rank 800→12000 主候选 8→1；化学→生物 主候选 5→空；**非写死**）。
- **差距过大分离**：`rank_diff > +1500` 进 `out_of_reach`「不推荐」折叠组（透明、不丢）。
- **伪登录 + 账户档案分桶隔离**：输用户名即登录；A 账户看不到 B 档案、增删/对话建档只影响当前账户桶；Supabase 在时兼容真实 Auth/RLS（MAO-21，`c8e557f`）。
- **再选≤2**：前端 + 后端（handlers 层 `secondary>2→info_insufficient`）拦截均已合 main（`a3ff51d`）。
- **官方来源入口**：每张候选卡可打开官方链接并查看更新时间。
- **6 步端到端**：API 调真引擎，非 mock / 非录像；**确认导出**生成带来源的 Markdown 行动方案。

### ⚠️ 样本 / 占位（数据层面，已逐项标注）

- **最低位次 `min_rank`**：3666 卡中仅 61 条手填基线卡有值，3621 条 null 标「位次待一分一段表派生」——一分一段表为图片、工具链暂无 OCR；引擎走投档分差兜底（MAO-26）不阻塞，位次后续补精。
- **招生计划 / 学费 / majors / 城市 / 层次**：待《招生计划》回填（当前 `plan/tuition` 部分缺，规则不阻断、标 caveat）。
- **2024 投档分**：现以 2025 为主，2024 回填中（maozh2 口径：2024/2025 主依据，2023 趋势参考）。

### 🧪 进行中 / 后置增强（明确标注）

- **§三 ④ 计划版本（关键日期 / 风险提醒）**：重算 + 导出已有，**关键日期（用「待公布」占位）+ 风险提醒补强 进行中**（全栈）。
- **MAO-26 投档分差兜底分档**：`min_rank` 空时按考生分 vs 卡投档分差分档，解锁 3666 卡冲稳保；reason/概率标注用哪种方法（AI工程师 进行中）。
- **一分一段表 → `min_rank` 回填**：受 OCR 墙限制，作为后续补精（不阻塞当前推荐）。
- **LLM 自然语言增强层**：maozh2 决定**不用 LLM key**；对话建条件走确定性层已可用，LLM 仅作可选锦上添花（非必需）。
- **真实 Supabase Auth / RLS**：当前为**伪登录 + 本地账户分桶隔离**（验收口径：账号/档案数据隔离真实生效即可）；生产级真实 Auth/RLS 为可选增强。
- **后端「再选不超过 2」拦截**：已合 main `a3ff51d`（输入校验层，不动 per-card 候选集）。
- **手机端真机视觉**未单独跑（响应式 Tailwind 已实现，主页面无明显错位；如实标注，不构成缺陷）。

### 受限原因与后续计划

- 对话建条件已用**确定性层实现（不用 LLM key）**；登录/多账户隔离已用**伪登录 + 本地账户分桶**达到验收口径（账号/档案隔离真实生效）。
- **仍待补**：§三④ 计划版本关键日期/风险提醒（全栈）、MAO-26 投档分差兜底（AI工程师）、一分一段表→`min_rank` 回填（OCR 墙）、2024 投档分 / 学费 / 计划 / majors 回填（随官方发布）。
- 受**官方数据可得性**（一分一段表图片 / 组级计划）限制，部分字段标 `待核`/`null`；计划随官方发布持续回填。

---

## 6. 测试记录

> 对应 DELIVERY §一-5 / TASK-SPEC §7「代表性测试案例」。详见 [`docs/QA-TEST-PLAN.md`](./QA-TEST-PLAN.md) 与 MAO-19。

### 6.1 一次完整流程

- **输入**：江苏 2026 物理类 · 637 分 · 位次 5,200 · 选科 物+化 · 学费预算 ≤ 60000。
- **结果**：资格过滤后候选若干（随当前 3666 卡数据集，按位次/选科/预算匹配）；冲/稳/保 + 差距过大分组；每条带 `source.url/publisher/updated`，概率只标 `method` + 年份，无"保证录取"。
- **自动化**：`vitest` 73/73、`npm run qa`（`scripts/qa-6step.mjs`，数据感知断言、与运行时同源读 `data/`、含选科维度）exit 0。

### 6.2 一个常见失败/变化场景

- **改条件**：位次 5,200 → 4,000，`SEU-08` **跨档**进入 diff（`added/changed`），候选与档位随之变化——证明非写死。更强验证见 §5/QA 实测：rank 800→12000 主候选 8→1；化学→生物 主候选 5→空。
- **异常路径**：缺关键条件 → `info_insufficient`；无候选通过 → `no_result`（均给原因 + 下一步）。

### 6.3 过程中发现的问题与修改

- **契约不一致**（MAO-16）：`rules.example.json` ↔ `engine.ts/types.ts` 的 `machine.type` 命名/params 形状对不上 → 资格过滤空转。**修复**：以 `types.ts` 为准对齐（数据 fff0a87），E2E-03 解阻。
- **6 步红线缺口**（MAO-18）：首版 `ChatPane` 占位、无导出 → **修复**：建条件表单 + 改条件随动 + 确认导出（合并 `main` 58aa00e）。

---

## 7. 验收结论（对照 TASK-SPEC §6 + DELIVERY §二）

| 评估标准 | 结论 |
|---|---|
| **问题、数据与规则** | 场景/用户清楚；官方数据 + 规则支撑资格判断与方案解释 ✅ |
| **产品与交互** | 筛选/比较/修改/重算/导出连续流程；关键依据与异常状态页面可见 ✅ |
| **使用结果** | 代表性案例完整运行；改关键条件后候选/方案/依据随之更新 ✅（已验证 diff） |
| **真实运行与后端支撑** | 真引擎 + 真数据；换输入得对应结果；显示处理状态/失败原因 ✅ |
| **基本稳定可用** | 异常有提示；伪登录 + 账户档案分桶隔离已上（MAO-21） |
| **可运行可交付** | README 写明启动/部署/已完成范围；模拟功能已标注 ✅ |

**三交付物**：① vercel.app（全球可访问，国内可达性按范围决策不做，见 §8）② 源码 + README（`main` `57a7d01`）③ 本报告（验收后定稿）。

> **最终验收待办（maozh2 明天真人验收）**：线上重点验 ①对话建条件 → 每轮更新考生档案（输 637/5100 看档案同步）②伪登录 A/B 账户档案互不可见 ③物理+历史两类候选随分/选科变化 ④多因素推荐理由（位次+选科+预算）。pending：MAO-26 投档分差兜底（解锁 3666 卡冲稳保）、③计划版本关键日期/风险提醒——这两块完成后再复验一次最顺。

---

## 8. 范围决策说明（有据取舍，非漏交）

> 诚实标注本次主动做出的范围取舍，便于评审判断为"有据决策"而非"漏交"。

1. **仅做高考志愿，不含两项共同必做工作流**（游戏营销 / AI 漫剧）：maozh2 确认"只需要完成这个升学 agent"，两项共同工作流后续独立处理（TASK-SPEC §7 已注明）。
2. **国内可达性不做**：默认 `*.vercel.app` 域名即算交付物 1「可直接访问」（已部署、全球可访问）；国内 DNS 对 `*.vercel.app` 有污染，但**不扩到自定义域名 / Cloudflare / 备案**（超出 MVP，maozh2 拍板"不需要国内可达性"）。按**非范围**处理，不写成"已知缺陷"。
3. **手机端真机视觉未单独跑**：响应式 Tailwind 已实现、主页面无明显错位；如实标注，不构成缺陷。
4. **LLM 对话 / 登录隔离列为后置**：核心 6 步零环境变量可跑通；增强项明确标注、未作为已实现。
5. **后端「再选不超过 2」拦截 = 做**（maozh2 seq236 拍板，推翻团队 unanimous「保持前端拦截」建议）：PM 范围界定为**输入校验层**——后端校验「考生提交的再选不超过 2 门」（`secondary>2` 拒、允许 1–2 门；前端被绕过时兑底），**不改 per-card 资格判定、不过滤卡、不动候选集**，使拦截诉求与候选随动两个都成立。用 `>2` 而非 `===2`：样本/测试含 `secondary=1` 门用例，恰好 2 会误阻（群管/AI工程师收敛定 >2）。
6. **物理类 + 历史类两类都做**（maozh2 要"覆盖大部分考生"）：原 PRD 仅物理类，扩到历史类；两类各 ≥100 所（现物理 996 校 / 历史 803 校，均超额）。
7. **登录用伪登录、但账户数据隔离必须真做**（maozh2 seq302）：不强制生产级真实 Auth；验收看"不同账户/不同考生档案数据不串"。已用伪登录 + 本地账户分桶实现（MAO-21）；真实 Supabase Auth/RLS 为可选增强。
8. **对话建条件 = 确定性层、不用 LLM key**（maozh2 seq273）：`/api/condition-building` 确定性抽条件/追问/冲突检测即满足 §三①；LLM 仅作可选锦上添花，不设为硬依赖。
9. **数据走官方 PDF 批量解析**：全栈用 pymupdf 解析 jseea 2025 投档线 PDF（物理 2522 / 历史 1155 条）→ converter 转卡，取代 web 逐校爬；守红线：只抽取不编造，位次/2024/学费/计划 待核全标注。
10. **推荐年份以 2024/2025 为主**（maozh2 seq325）：2026 推荐主依据 2024/2025 投档数据；2023 仅趋势参考；卡片标清年份/来源/待核，不得把 2026 说成真实录取结论。
11. **`min_rank` 空时按投档分差兜底分档**（MAO-26，进行中）：一分一段表为图片、工具链暂无 OCR，位次回填受阻；引擎在 `min_rank` 空时按考生分 vs 卡投档分差分档，先解锁 3666 卡冲稳保，位次作后续补精，reason 标注用哪种方法不混淆。

---

## 9. AI 协作

> 对应 DELIVERY §一-6。

- **AI（5 个智能体）参与**：调研、方案、PRD、数据采集与结构化、规则整理、引擎/前端/API 代码、调试、测试（6 步回归 + 单测）、部署配置、文档。
- **人工（maozh2）判断并拍板**：范围（省份/年度/用户类型 B/不含共同工作流/国内可达性不做）、技术栈确认、Vercel 账号与部署执行、关键节点验收口径。
- **三红线由人 + agent 共同守住**：只规划不承诺录取；官方事实 / 规则判断 / AI 建议分层；最终以官方页面为准。

---

## 10. 成员分工（人 + 5 agent 协作）

> 本项目为单人完成；下列分工由 maozh2 + 5 个智能体协作填写。**各角色按「已实现 / 部分实现 / 未实现」交一段，PM 整合。**

### 10.1 项目经理 PM（升学规划PM）
- **已实现**：范围界定（江苏 2026 物理类，不含博士/留学/两项共同工作流）；PRD-v1（三类用户 + 规划人员多客户 B + 登录隔离 + MVP 边界，合并 `main` c1f8dcb）；里程碑与 issue 拆分指派（MAO 系列）；验收口径（交付物 1 vercel.app + 国内可达性范围决策）；本报告统稿（交付物 3）。
- **部分实现**：PRD 中登录/多账户隔离为规划层，落地属全栈/AI 工程师后置。
- **未实现**：不写产品代码（按角色边界）。

### 10.2 AI Agent 工程师（决策核心）
- **已实现**：TS 纯函数决策引擎——资格校验 `checkEligibility`（canonical 4 类规则，per-card `cardSubjectOk` 首选+再选硬过滤）、方案比较 `compareFiltered`（先过资格再排序）、改条件重算 `/api/recompute`、`out_of_reach` 差距过大分离；**§三① 对话建条件引擎** `/api/condition-building`（确定性层：抽条件 / `findMissingConditions` / 冲突检测 / 追问，MAO-23，合 `5ea5acc`/`866e255`）；**多因素推荐理由** `buildReason`（位次+选科+预算+年份 trace，`17fe3e9`，vitest 73/73）；决策透明 `trace`；typecheck 净。
- **已推待合 main**：MAO-26 `min_rank` 空时按投档分差兜底分档（`ai-eng/score-fallback` `bd384a1`，npm test 78/78），解锁 3666 卡冲稳保。
- **未实现**：LLM 增强层（maozh2 决定不用 key，非必需）。

### 10.3 全栈开发工程师
- **已实现**：数据模型 + RLS + Auth 基座（`prisma/schema.prisma`：Account→Profile→Plan→Version）；前端骨架 + 候选卡；红线补全——建条件表单 / 改条件随动 / 确认导出 Markdown（MAO-18）；compare `groups/out_of_reach` 联调 + 差距过大折叠；**§三① 对话建条件 UI**（ChatPanel 接 `/api/condition-building`，MAO-24，`866e255`）；**§三③ 两套方案并排**（MAO-24，`aa090af`）；**对话每轮更新考生档案**（MAO-25 返工，`3b4584c`）；**伪登录 + 账户档案分桶隔离**（MAO-21，`c8e557f`）；**jseea 投档线 PDF 解析器**（pymupdf，物理 2522 + 历史 1155 条，供数据转卡）；再选≤2 前端拦截；部署 vercel.app（MAO-17）；README 交付物 2（MAO-20）。
- **进行中**：§三④ 计划版本关键日期（「待公布」占位）+ 风险提醒补强；候选冲/稳/保分组。
- **未实现**：手机端真机视觉单独跑。
- **过程教训**：一次 UI 块自查自合 `main`（`3ff4923`，未走 PR）；已纠正，后续一律 PR + 群管理复核再进 main。

### 10.4 数据与规则研究员
- **已实现**：官方数据采集结构化——**双类 3666 卡**（物理 2511/996 校 + 历史 1155/803 校），由 jseea 2025 投档线 PDF 批量解析 + converter 脚本转卡，来源全标官方 URL；每卡 `subject_requirement` 首选+再选格式（引擎 per-card 直消费）；硬条件规则表（canonical 4 类 machine 判据）；qa-6step 数据感知断言、与运行时同源读 `data/`。
- **样本占位**：`min_rank` 3621 条 null（一分一段表为图片、OCR 墙）；2024 投档分 / 学费 / 计划 / majors / 城市 待《招生计划》回填。
- **未实现**：无（数据层无硬阻塞；随官方发布持续回填）。

### 10.5 QA / 测试工程师
- **已实现**：测试计划（MAO-10，4 AC + 三红线 + 异常 EX-1~5）；6 步端到端回归脚本 `scripts/qa-6step.mjs`（exit 0）；硬断言对齐 TASK-SPEC §6——改分/改选科 **候选集合（非档位）** 变化（实测：rank 800→12000 主候选 8→1；化学→生物 主候选 5→空）；per-card 选科黑盒覆盖（03b：物理+生物考生→仅 SEU-07 通过）；tests/fixtures 与运行时同源修复（`eed595f`）。
- **部分实现**：本地等价回归全绿；线上 vercel.app 国内连不上，端到端那一截待可达环境 / 真人。
- **未实现**：multi-profile 隔离验收（待 Supabase anon+service_role 凭证直打 RLS）。

### 10.6 群管理（整体协调）
- **已实现**：整体协调主导——审（对照可验收规格核验，独立跑 typecheck / vitest / build / qa）、派（带验收标准的 issue 派活）、催（每小时巡检）、合并 / 去重（收口早期 issue 重复风暴）；MAO-21 umbrella（候选随动 + multi-profile 收口）；issue 回退流程（验收发现缺陷 → 对应 done issue 回退 in_progress → 派人修 → QA 复测）。
- **边界**：协调与验收，不下场实现产品代码。
- **本轮 main 推进到 `57a7d01`**（对话引擎 + 对话建档返工 + 多因素 reason + 伪登录隔离 + 双类 3666 卡）独立验证。

---

## 附：交付物索引与关键 commit

| 交付物 | 位置 / commit |
|---|---|
| ① 可访问网页 | <https://ai-school-admission-planning-agent.vercel.app/>（MAO-17） |
| ② 源码 + README | `main` = `57a7d01`（双类 3666 卡 / 对话建档 / 多因素 reason / 伪登录隔离 / 后端拦截 / 报告） |
| ③ 报告 | `docs/REPORT.md`（本文件，`pm/report-update`） |

**关键 commit**：PRD `c1f8dcb`/数据口径 `959a91a` · AI 引擎 per-card 选科 `16e8c1e` · 后端拦截 `a3ff51d` · **对话建条件引擎 MAO-23** `5ea5acc`/`866e255` · **对话建档返工 MAO-25** `3b4584c` · **多因素 reason** `17fe3e9` · **伪登录隔离 MAO-21** `c8e557f` · **物理 bulk** `c8e557f` · **历史类 bulk** `57a7d01` · qa-6step 数据感知 `3ea4558` · 报告本批 `pm/report-update`。
